# Destination number → orchestrator profile_id
# Copy to /etc/coraltele/sipserver/scripts/ai_profiles.conf
# Lines:  DEST=profile   or   DEST  profile
# Channel var ai_profile_id still wins if set.

101=coral-xfer
